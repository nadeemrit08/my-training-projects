server:
  port: 3333
  
spring:
  application:
    name: GATEWAY-API
  main:
    web-application-type: reactive         
eureka:
  client:
    service-url:
      defaultZone: ${DISCOVERY_URL:http://localhost:9090}/eureka/  

    